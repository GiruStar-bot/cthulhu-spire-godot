# Abyss of R'lyeh 開発引き継ぎ資料 v2（詳細ルール集）

このプロジェクトの新しいチャットは、この資料を前提に会話を始めてよい。v1（プロジェクト概要中心）を土台に、**試行錯誤で確立したルール・地雷・省略禁止事項**を大幅加筆した版。

---

## 1. プロジェクト基本情報

- **タイトル**: Abyss of R'lyeh（作業名）。クトゥルフ神話 × Slay the Spire風デッキビルド × 探索ローグライク
- **公開先**: https://girustar-bot.github.io/cthulhu-spire/ （GitHub Pages自動デプロイ、GitHub Actionsで`typecheck`+`build`を自動チェック済み）
- **リポジトリ**: GiruStar-bot/cthulhu-spire (branch: main、公開リポジトリ)
- **開発者**: Yamamoto Luca（宗一郎）
- **スタック**: React 19 + Vite 6 + Tailwind v4 + Zustand 5 + TypeScript

## 2. 3ツール体制（コスト最適化のため厳守）

- **Claude（このチャット）**: 設計・仕様策定・Grok向け実装プロンプト作成・Gemini向け画像生成プロンプト作成。**このリポジトリへの書き込み権限は持たない**（`curl`で公開リポジトリのtarballを取得して閲覧するだけ。コード実行機能がオフのチャットではこれもできない）。
- **Grok**: 実際のコード実装・GitHubへのコミット＆プッシュ（コスト最適化のため意図的にこの役割）。
- **Gemini**: 画像・ドット絵生成（`gemini.google.com`のチャット画面）。
- 実装は必ず「Claudeが詳細なMarkdown/XML形式のプロンプトを作る→ユーザーがGrokに貼る」という手順を経る。Claudeが直接コードを書き換えることはできない。

---

## 3. 厳守すべきルール・避けるべき実装・省略してはいけない手順

### 3-1. データ構造・セーブデータまわり（最重要）

1. **永続化データに新しいフィールドを足す時は、必ず`?? {}` / `?? []` / `?? 0`等のフォールバックを入れる。** 旧セーブデータにはそのフィールドが存在しないため、フォールバック無しだと本番で確実にクラッシュする。過去に`profile.equipped`, `EquipmentInstance.bonusStats`等で実際にこの対策を入れている。
2. **`useCollectionStore`（カード・ルーン・装備・デッキ、localStorage）と`profile`/ゲーム本体状態（`game/store.ts`、HP/SAN/現在階層/装着中装備スロット等）は別々の永続化層。** 混同して片方だけ更新すると同期が壊れる。
3. ファイル・モジュールを削除する前に、**そこに定義されている関数が他からimportされていないか必ず確認する。** 例：`relics.ts`を削除する際、`tierFromFloor`関数だけは`runes.ts`が依存していたため、先に`equipment.ts`へ移設してからでないと壊れた。
4. データ構造の型（例：`deck: string[]`→`decks: Record<name, Record<cardId,count>>`のような刷新）を変更した時は、**その型を使っている全関数のシグネチャ（例：`copiesOfBase`）を洗い出して更新する。** 呼び出し元の変更漏れは型チェックで検出できるので、必ず`npm run typecheck`を通してから完了とすること。
5. **1回計算してその後変わらない値（例：`CombatState.equipmentStats`は`startCombat`時に1回だけ計算）と、リアルタイムに変わる値を混同しない。** 装備を戦闘中に変えても、その戦闘の間は反映されない（次の戦闘から反映）。ユーザーがこれを「バグ」と誤解した実例がある。実際に不具合を疑われたら、まず計算パイプライン全体（型定義→計算関数→適用箇所）をコードで再確認してから答える。**憶測で「直っていないはず」と決めつけて再修正のプロンプトを出さない。**

### 3-2. 大きな変更の進め方

1. **依存関係の強い変更は1本のまとまったプロンプトにする。無関係な変更は分割する。** 遺物廃止＋装備新設＋カード魔改造廃止は、中途半端に片方だけ消すと壊れるため意図的に1本で実施した。逆に独立したUI修正（戦闘ログ・デッキ選択パネル等）は分割して進めた。
2. **新機能はまず動く状態で追加し、実プレイを見てから数値・バランスを調整するスタイルで進める。** 完璧な数値を最初から狙わない（叩き台優先）。
3. ある概念（例：アーキタイプの見た目）を仕様変更する時は、**関連する全アイテム・全プロンプトを同時に直す。** 1つだけ直して残りを放置すると、シリーズ内で見た目がちぐはぐになる（狂信者5点の再設計で経験済み）。
4. 複数箇所に同じガード条件を入れる必要がある場合（例：正気0の「全ロスト」トリガーは実は4箇所に分散している）、**1箇所直して満足せず、grepで全呼び出し箇所を洗い出してから対応する。**

### 3-3. UI/UXの確立済み方針

1. **「戻る」ボタンは画面右下に統一する**（複数回の指示で確立したルール。新しい画面を作る時もこれに従う）。
2. **文字で説明しすぎないUIを志向する。** 演出的な説明文より、最小限のラベル＋ビジュアルを優先する（例：正気0画面「LOST THE ALL」は見出し＋ボタンのみで説明文なし）。
3. `CardView`（カード共通コンポーネント）の枠線は`aiTag`/`archetype`で色分け済み：**攻撃=赤、防御=青、効果=紫**。このコンポーネントは手札・敵の使用カード表示・ショップ等、全箇所で共有されているため、**変更するとゲーム全体に伝播することを常に意識する。**
4. 戦闘ログの色分け規則（キーワード判定）：ダメージ=赤(blood)、ブロック=水色(sky-300)、回復=緑(green-300)、正気=シアン(cyan-300)、状態異常(弱体/脆弱/毒/恐怖)=紫(purple-300)。
5. 新しいパネルを追加する時、**既存の絶対配置要素と重ならないか必ず確認する。** 過去にデッキ選択ボタンが探索準備画面の「潜航開始」エリアと重なる不具合が発生し、専用パネルとして独立させて解決した。

### 3-4. ゲームデザイン上の確立済みルール

1. **最大HP・最大正気の増加は「ステ振り」のみに限定**し、装備・ルーン由来のボーナスは意図的に禁止している（バランス上の明示的な決定）。
2. **敵AIのカードプールには、プレイヤー視点の条件付き効果（例：「敵の意図が攻撃なら追加ダメージ」）を持つカードを含めない。** 敵が使うと条件が評価されず、本来より弱い劣化版になってしまうため（`precise`, `tome`等は`AI_EXCLUDED_IDS`で除外済み）。
3. 敵は`tier: "mob" | "elite"`でレア度ガードがかかる（雑魚はstarter+commonのみ、エリートはuncommonまで）。**ボスは`EnemyDef.deck`で明示的にカードリストを指定し、このレア度ガードの対象外**。
4. `biomeForEncounter()`は、戦闘に登場する敵の**`EnemyDef.biome`フィールドを見て背景を決める**（優先順位：colour > void > shrine > 通常バイオーム）。**この仕組みは出現頻度（どの階層でどのくらいの確率で出るか）とは完全に独立している。** 見た目だけ変えたい場合は`biome`フィールドだけ変更すればよく、出現テーブル自体は触らなくてよい（byakheeをvoid背景にした際、出現頻度を変えずに済んだ実例）。
5. `dread`（恐怖）・`frostbite`（凍傷）は、**引いた瞬間に効果発動→そのまま手札に残る**（即時廃棄システムは完全廃止済み）。手札を圧迫すること自体がペナルティという設計意図。将来的に「手札にあるだけで効果を発揮するカード」（例：盾カード）を作る布石でもある。
6. ボス専用カード（`signatureCardId`）の**レアドロップ機構は完全廃止済み**。ボス専用カードは敵AI専用で、プレイヤーが入手する経路は存在しない。
7. カードのルーン魔改造（個体差）は完全廃止済み。**ルーンは装備にのみ装着可能。** これによりカードが完全に均一になり、デッキのテンプレート方式（複数デッキ管理）が実現できた。
8. ルーンカタログの`ATK+`/`COST-`は廃止（`STR+`に統合、画像資産のみ温存）。現存6種：`BLK+`(防御%), `DRAW`(1ターンの引く枚数+n), `SAN+`(狂気耐性%), `STR+`(筋力), `POISON`(毒耐性%), `HEAL`(毎ターン自動回復)。
9. 装備は5部位（頭/胸/腕/脚/足）を同一アーキタイプで統一すると加護発動。**現在実装済みは狂信者（正気0でも全ロストしない）と毒（毒完全無効+回復量+50%）のみ**。他6ジャンル（騎士/外宇宙/旧神/深きもの/供物/影）は未実装。

### 3-5. Gemini画像生成の鉄則（全て実際の失敗から学んだもの）

1. **腕・脚など左右対になる部位は、必ず"a pair of"と明記する。** 省略すると片方しか生成されない（実際に発生：`patched_legs`等が片脚のみで生成された）。
2. **背景は"no floor, no wall, no environment whatsoever, pure empty darkness like a game inventory icon"まで明記する。** 単に"dark background"とだけ書くと、床や壁が生成されるものとされないものが混在する。
3. **布製アイテムは"NOT tailored modern clothing. No structured lapels, no tailored collar, no symmetric pockets, not a trench coat, crudely hand-stitched, asymmetric, ragged edges"を明記する。** 省略すると現代的なトレンチコート風になってしまう（実際に発生）。
4. **脚防具は"show only the knee-to-shin area, cropped just above the ankle, do not include any foot, boot, or sabaton coverage"を明記する。** 省略すると足先まで描かれ、別枠の靴と絵が重複する。
5. キャラクター単体イラストは"Solo, single character only, no other characters in frame"「台座・地面・影・小道具なし」「NOT chibi」「正面向き・左右対称」を基本ルールとする。背景はマゼンタ単色RGB(255,0,255)（背景除去前提のキャラのみ。装備アイコンは背景除去不要なので黒背景でよい）。
6. 実在の商標的な意匠（例：エルダーサインの記号そのもの）は**オリジナルの意匠として生成する**よう明記し、直接模倣は避ける。
7. ある装備・キャラのコンセプトを刷新する時（例：狂信者を防具系→赤いローブ系に変更）は、**シリーズ内の全アイテムを同時に直す。** 1点だけ直すと統一感が崩れる。

### 3-6. Gemini画像生成の自動化（Claude in Chrome使用時）

1. **プロンプト入力後、Enterキーでは送信されないことがある。** 必ず送信ボタン（紙飛行機アイコン）を直接クリックする。
2. **クリック→入力が空振りすることがある**（スクリーンショットで空欄のまま確認できる）。入力後は必ずスクリーンショットで反映を確認し、空なら`triple_click`→`ctrl+a`→再入力でリトライする。
3. 生成のたびに`gemini.google.com/app`へ`navigate`し直し、新しいチャットから始める方が、過去の会話に紛れ込む事故（実際に発生：無関係な過去チャットに切り替わった）を避けられる。
4. ダウンロードしたファイルはGeminiの自動生成ファイル名になり、装備/カードid通りの名前にはならない。**リネームは手動作業として残る**（Claudeにはブラウザのダウンロードフォルダを直接操作する権限が無い）。

### 3-7. インフラ

- GitHub Actions（`.github/workflows/ci.yml`）が、プッシュ毎に`npm ci`→`typecheck`→`build`を自動実行する。CIジョブが増えた分、Actionsタブ全体の体感時間は増えるが、これは意図した動作で異常ではない。
- 画像素材の反映は`scripts/apply-art.mjs`（`npm run apply-art -- --kind cards|enemies|equipment`）で自動化済み。新しい素材カテゴリを追加する時は、この`KIND_CONFIG`に追記する形にする（個別スクリプトを都度書かない）。

---

## 4. 完成している主要システム（現状のサマリ）

1. **戦闘システム**: カードプレイ、敵AI（カードベース、`rollEnemyCard`、tierごとのレア度制限）、ボスは専用デッキ＋1ターン2枚使用
2. **装備システム**: 5部位、tier1-5＋`power`乱数＋`bonusStats`（tier依存の追加ボーナス確率）、ルーン装着、2ジャンルの加護実装済み
3. **デッキ管理**: テンプレート方式、複数デッキ、探索準備画面で選択
4. **10層チェックポイント**: `resumeDescent`/`extractToHub`
5. **村落**: 建物クリック式マップ、鍛冶屋でカード＋装備販売
6. **戦闘ログ**: 右上パネル、5行常時＋全履歴モーダル、色分け済み
7. **カード枠線色分け**: 攻撃=赤/防御=青/効果=紫
8. **恐怖・凍傷**: 手札残留式（即時廃棄なし）
9. **GitHub Actions CI**: 稼働中

## 5. 未着手・今後の課題

1. 防御%→絶対数値化、ダメージ計算をそれに追従（着手予定だったが実施状況は要確認）
2. 各装備のステータス内訳表示のさらなる拡充
3. ルーンの種類増加＋各ルーンへの副次効果付与
4. 各アーキタイプに最低4種のカードを実装（現状ジャンルによって手薄）
5. エネミー強化：10層区間内での段階的強化、雑魚・エリート専用デッキ
6. 鍛冶屋の旧カードショップ（`SHOP_CARDS`）をアーキタイプ体系に統合するか整理
7. 残り6ジャンル（騎士/外宇宙/旧神/深きもの/供物/影）の5部位統一加護
8. デッキ20枚中16枚同ジャンルのシナジー判定（保留中）

## 6. 過去の詳細経緯について

キャラクター/敵のアートディレクション、村落UI改修の変遷、遺物システムの当初設計等、この資料に書ききれない細部は過去のチャットに残っている。「過去のチャットを検索して」と伝えれば、Claude側の会話検索機能で該当履歴を探し出せる。
